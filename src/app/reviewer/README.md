# src/app/reviewer

Purpose: Reviewer profile and related pages.

Key routes

- `[id]/` — reviewer profile detail (see `page.tsx`).
- `parts/` — shared UI fragments for reviewer pages.

Dependencies

- Supabase data for profiles/reviews, SWR hooks for fetching, shared UI from `src/app/components`.

Owner: TODO.
