# IconButton

Top-level icon button — delegates to `atoms/IconButton` or wraps a native button.

## Files

| File             | Description |
| ---------------- | ----------- |
| `IconButton.tsx` | Main export |

## Notes

This is a feature-level wrapper. For the design system primitive, see `atoms/IconButton/`.
