# PasswordChangeForm

Form for changing a user's password (current → new → confirm).

## Files

| File                     | Purpose          |
| ------------------------ | ---------------- |
| `PasswordChangeForm.tsx` | Main export      |
| `index.ts`               | Barrel re-export |

## Used By

Profile security settings page.
