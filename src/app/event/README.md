# src/app/event

Purpose: Event detail routes and supporting UI.

Key route

- `[id]/` — event detail page (see `page.tsx`).

Data sources

- Supabase events data; Ticketmaster/Quicket ingests are currently disabled — seed data manually or provide a replacement ingest path if re-enabled.
- Map/display assets pulled via hooks from `src/app/lib`.

Owner: TODO.
